EpitopeMatcher v0.2
-----------------------------------------------------------
Rename functions and data structures to have more meaningful names.

Only compute similarity scores for those sequence / epitope combinations where
it is exlicitly requested.

EpitopeMatcher v0.1
-----------------------------------------------------------

Fuzzy matching is now supported between the patient_hla and query_alignments
as well as between the patient_hla and lanl_hla files.
